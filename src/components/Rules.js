import React from 'react';

const Rules = (props) => {
    return(
        <div className="container">
        <h3>Click on an image to get a point. Each click shuffles the images. If you click on the same image a second time, the game is over!</h3>
        </div>
    )
}

export default Rules;